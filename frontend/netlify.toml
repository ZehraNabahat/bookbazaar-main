# Used only if Netlify "Base directory" is set to frontend in the dashboard
[build]
  command = "npm ci && npm run build"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
